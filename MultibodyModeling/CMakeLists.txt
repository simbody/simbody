FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES *.h)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/MultibodyModeling ${PROJECT_SOURCE_DIR}/CDSlib)
ADD_LIBRARY(MultibodyModeling ${SOURCE_FILES} ${INCLUDE_FILES})
