FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES *.h)

ADD_EXECUTABLE(JunkMain       ${SOURCE_FILES} ${INCLUDE_FILES})
ADD_EXECUTABLE(JunkMainStatic ${SOURCE_FILES} ${INCLUDE_FILES})

TARGET_LINK_LIBRARIES(JunkMain       ${SHARED_TARGET})
TARGET_LINK_LIBRARIES(JunkMainStatic ${STATIC_TARGET})