FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES *.h)

ADD_EXECUTABLE(ContactTest       ${SOURCE_FILES} ${INCLUDE_FILES})
ADD_EXECUTABLE(ContactTestStatic ${SOURCE_FILES} ${INCLUDE_FILES})

TARGET_LINK_LIBRARIES(ContactTest       ${SHARED_TARGET})
TARGET_LINK_LIBRARIES(ContactTestStatic ${STATIC_TARGET})